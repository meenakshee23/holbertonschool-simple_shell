# holbertonschool-simple_shell
This is my repository as a full-stack engineer / me

A simple UNIX command line interpreter.

## Usage
Compile:
```bash
gcc -Wall -Wextra -Werror -pedantic simple_shell.c -o simple_shell


/////////

simple_shell \- simple UNIX shell
.SH SYNOPSIS
./simple_shell
.SH DESCRIPTION
simple_shell is a minimalist UNIX command line interpreter. It executes commands typed by the user, searches the PATH, and supports built-ins.